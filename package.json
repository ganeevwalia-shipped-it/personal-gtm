{
  "name": "personal-gtm",
  "version": "1.0.0",
  "description": "Personal GTM engine — nightly content drafting from Google Sheets → Claude API → Notion",
  "type": "module",
  "scripts": {
    "draft": "node src/main.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@notionhq/client": "^2.2.0",
    "googleapis": "^144.0.0",
    "dotenv": "^16.4.0"
  }
}
